# LazyQMK Docker Compose Environment Variables
# Copy this file to .env and customize as needed

# =============================================================================
# QMK Firmware Path (Required)
# =============================================================================
# Path to QMK firmware directory on host machine
# This is required for:
#   - Reading keyboard geometry (info.json)
#   - Generating firmware files (keymap.c, keymap.json)
#   - Compiling firmware binaries (.hex, .bin, .uf2)
#
# Default: ./qmk_firmware (git submodule)
# Size: ~1.1 GB
#
# Setup Options:
#   1. Use git submodule (recommended):
#        git submodule update --init --recursive qmk_firmware
#
#   2. Use external QMK clone:
#        git clone --recurse-submodules https://github.com/qmk/qmk_firmware.git ~/qmk_firmware
#        QMK_FIRMWARE_PATH=~/qmk_firmware
#
#   3. Use custom QMK fork (for full LazyQMK features):
#        git clone --recurse-submodules https://github.com/Radialarray/qmk_firmware.git ~/qmk_firmware
#        QMK_FIRMWARE_PATH=~/qmk_firmware
#
# See docs/DOCKER_QMK_SETUP.md for detailed setup instructions
#
QMK_FIRMWARE_PATH=./qmk_firmware

# =============================================================================
# Workspace Directory (Optional)
# =============================================================================
# Path to directory containing your layout markdown files
# Default: ./examples (included example layouts)
#
# Examples:
#   - Use your custom layouts directory:
#       LAZYQMK_WORKSPACE=~/my-keyboard-layouts
#
#   - Use default examples:
#       LAZYQMK_WORKSPACE=./examples
#
LAZYQMK_WORKSPACE=./examples

# =============================================================================
# Config Directory (Optional)
# =============================================================================
# Path for LazyQMK configuration files
# Default: lazyqmk-config (named Docker volume)
#
# Options:
#   - Use Docker volume (default, managed by Docker):
#       LAZYQMK_CONFIG_DIR=lazyqmk-config
#
#   - Use host directory (for direct file access):
#       LAZYQMK_CONFIG_DIR=~/.config/LazyQMK
#
#LAZYQMK_CONFIG_DIR=lazyqmk-config

# =============================================================================
# Port Configuration (Optional)
# =============================================================================
# Override default ports for services (uncomment to customize)
#
# Backend API port (default: 3001)
#BACKEND_PORT=3001
#
# Frontend port (default: 5173)
#FRONTEND_PORT=5173

# =============================================================================
# Logging (Optional)
# =============================================================================
# Rust log level for backend
# Values: error, warn, info, debug, trace
# Default: info
#
#RUST_LOG=info

# =============================================================================
# Notes
# =============================================================================
# - This file (.env.example) is a template - copy to .env and customize
# - .env is gitignored to avoid committing local configuration
# - See docker-compose.yml for how these variables are used
# - See docs/DOCKER_QMK_SETUP.md for comprehensive setup guide
